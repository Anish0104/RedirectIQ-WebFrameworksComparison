# Nginx Proxy Configuration

# Shows the expected environment variables for the Nginx-backed RedirectIQ benchmark target.
PORT=8003
NGINX_PORT=3003
JWT_SECRET=supersecretkey123
DB_PATH=./redirectiq.db
CACHE_TTL_SECONDS=60
PUBLIC_BASE_URL=
BENCHMARK_MODE=true

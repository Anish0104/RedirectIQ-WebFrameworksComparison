# Apache Proxy Configuration

# Shows the expected environment variables for the Apache-backed RedirectIQ benchmark target.
PORT=8004
APACHE_PORT=3004
JWT_SECRET=supersecretkey123
DB_PATH=./redirectiq.db
CACHE_TTL_SECONDS=60
PUBLIC_BASE_URL=
BENCHMARK_MODE=true

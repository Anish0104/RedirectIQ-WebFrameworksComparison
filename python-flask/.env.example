# Shows the expected environment variables for the Flask RedirectIQ benchmark target.
PORT=3002
JWT_SECRET=supersecretkey123
DB_PATH=./redirectiq.db
CACHE_TTL_SECONDS=60
PUBLIC_BASE_URL=
BENCHMARK_MODE=true
